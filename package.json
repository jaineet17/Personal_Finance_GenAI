{
  "name": "finance-rag-backend",
  "version": "1.0.0",
  "description": "Finance RAG API Backend",
  "private": true,
  "scripts": {
    "deploy:dev": "cd src/infrastructure && serverless deploy --stage dev",
    "deploy:prod": "cd src/infrastructure && serverless deploy --stage prod",
    "test": "pytest"
  },
  "dependencies": {
    "serverless": "^3.34.0"
  },
  "devDependencies": {
    "serverless-python-requirements": "^6.0.0",
    "serverless-dotenv-plugin": "^6.0.0"
  }
}
